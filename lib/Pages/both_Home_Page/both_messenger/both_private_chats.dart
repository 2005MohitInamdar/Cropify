import 'package:flutter/cupertino.dart';

class both_private_chat extends StatelessWidget {
  const both_private_chat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Both Private chats"),
    );
  }
}
