import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class both_collaboration_page extends StatelessWidget {
  const both_collaboration_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Both collaboration page"),
    );
  }
}
