import 'package:flutter/cupertino.dart';

class dealer_private_chat extends StatelessWidget {
  const dealer_private_chat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Dealer's Private Chats"),
    );
  }
}
