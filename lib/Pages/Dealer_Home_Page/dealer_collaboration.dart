import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class dealer_collaboration extends StatelessWidget {
  const dealer_collaboration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Dealer's collaboration page"),
    );
  }
}
